spring:
  datasource:
    url: jdbc:postgres://localhost:5432/postgres
    username: nidhibajaj
    password: nidhibajaj
    hikari:
      schema: public
  jpa:
    hibernate:
      ddl-auto: none
  flyway:
    baselineOnMigrate: true
    schemas: public
    locations: classpath:db/migration

server:
  port: ${PORT:8080}
