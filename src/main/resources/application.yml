spring:
  datasource:
    url: jdbc:h2:mem:hotelmenudb
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
server:
  port: $(port:8080)

